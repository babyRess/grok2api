meta {
  name: Count tokens
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/v1/messages/count_tokens
  body: json
  auth: none
}

headers {
  content-type: application/json
  x-api-key: {{apiKey}}
}

body:json {
  {
    "model": "{{model}}",
    "messages": [
      {
        "role": "user",
        "content": "Hello token counter"
      }
    ]
  }
}

docs {
  # Count tokens
  Local deterministic estimate (no upstream call).
}
